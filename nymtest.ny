#include "nytmash/nytmash.nyt" // custom lib

int main() {
    int x = add(1, 2);
    int y = frt(16);
    int z = mul(5, 6);

    print "x = ", x;
    print "y = ", y;
    print "z = ", z;

    return 0;
}
