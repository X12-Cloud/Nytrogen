// sys_write(fd, ptr, count) → bytes written
int sys_write(int fd, string data, int count) {
    int result;
    asm {
        "mov rax, 1"
        "syscall"
        "mov [rbp - 8], eax"
    }
    return result;
}