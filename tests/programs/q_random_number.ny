int main() {
    qubit q;
    int total_goes = 8;
    int byte[8];

    for (int i = 0; i < total_goes; i = i + 1) {
        h -> q;
        int result = (int)q;
        print result;
        byte[i] = result;
        if (result == 1) {
            x -> q;
        }
    }
    //print byte[1], byte[2];
    return 0;
}

